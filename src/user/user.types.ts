export enum Role {
  User = 'User',
  Admin = 'Admin',
}

export enum AuthProvider {
  Local = 'local',
  Google = 'google',
  Apple = 'apple',
}

export enum Gender {
  Male = 'male',
  Female = 'female',
  NonBinary = 'non_binary',
  Other = 'other',
}

export enum AccountStatus {
  Active = 'active',
  Suspended = 'suspended',
  Banned = 'banned',
}
